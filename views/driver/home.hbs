<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Driver Login</title>
    <style>
        /* Reset default browser styles for consistency */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling with a dark theme */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #000; /* Black background for a sleek look */
            color: #fff; /* White text for contrast */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Navbar styling */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: #121212; /* Slightly lighter black for visibility */
            padding: 15px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Navbar title styling */
        .navbar h1 {
            font-size: 1.5rem;
            margin: 0;
        }

        /* Login container styling */
        .login-container {
            background-color: #121212; /* Slightly lighter black for contrast */
            border-radius: 15px;
            padding: 60px;
            width: 100%;
            max-width: 400px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(255, 255, 255, 0.1); /* Subtle white glow */
            margin-top: 80px; /* Space for the navbar */
        }

        /* Main heading styling */
        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        /* Form styling */
        form {
            margin-top: 45px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        /* Input fields styling */
        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 12px 20px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            background-color: #222; /* Dark gray background */
            color: white;
            outline: none;
            transition: background-color 0.3s ease;
        }

        /* Focus effect for input fields */
        input[type="text"]:focus,
        input[type="password"]:focus {
            background-color: #333; /* Slightly lighter gray on focus */
        }

        /* Placeholder text styling */
        input::placeholder {
            color: #aaa; /* Light gray placeholder text */
            font-size: 1rem;
        }

        /* Submit button (Login) styling */
        button[type="submit"] {
            color: white;
            background-color: #6a0fd3; /* Purple accent for login button */
            font-size: 1rem;
            font-weight: bolder;
            padding: 12px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        /* Hover effect for submit button */
        button[type="submit"]:hover {
            transform: scale(1.05);
            background-color: #812be4; /* Darker purple on hover */
        }

        /* Sign-up button styling */
        .signup-btn {
            background-color: #ffffff; /* White background for sign-up button */
            color: rgb(0, 0, 0); /* Black text for contrast */
            font-size: 1rem;
            padding: 12px;
            width: 100%;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        /* Hover effect for sign-up button */
        .signup-btn:hover {
            transform: scale(1.05);
            background-color: #2ce600; /* Neon green on hover */
        }

        /* Error message styling */
        .error-message {
            color: #ff4d4d; /* Red text for error messages */
            margin-top: 10px;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <!-- Navbar with the title -->
    <div class="navbar">
        <h1>Driver Login</h1>
    </div>

    <!-- Main content area -->
    <div class="container">
        <!-- Login form container -->
        <div class="login-container">
            <h1>Driver Login</h1>
            <form action="/driver/login" method="POST">
                <input type="text" id="username" name="username" placeholder="Username" required>
                <input type="password" id="password" name="password" placeholder="Password" required>
                <button type="submit">Login</button>
            </form>
            {{#if error}}
                <p class="error-message">{{error}}</p>
            {{/if}}
            <br>
            <a href="/driver/register">
                <button type="button" class="signup-btn">Sign Up</button>
            </a>
        </div>
    </div>
</body>
</html>