<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        /* Reset default browser styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #000;
            /* Black background */
            color: #fff;
            /* White text */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Navbar styling */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: #121212;
            /* Slightly lighter black for contrast */
            padding: 15px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            border-bottom: 1px solid #333;
            /* Subtle border for separation */
        }

        .navbar h1 {
            font-size: 1.5rem;
            margin: 0;
            letter-spacing: 1px;
        }

        /* Main content styling */
        .content {
            text-align: center;
            margin-top: 80px;
            /* Space for the navbar */
        }

        /* Heading styling */
        h1 {
            font-size: 2.5rem;
            margin-bottom: 40px;
            letter-spacing: 1px;
        }

        /* Button container */
        .button-container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            align-items: center;
        }

        /* Button styling */
        .button {
            background-color: #6a0fd3;
            /* Default button color (purple) */
            color: white;
            font-size: 1.2rem;
            font-weight: bold;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            /* Rounded corners */
            cursor: pointer;
            text-decoration: none;
            text-align: center;
            transition: transform 0.3s ease, background-color 0.3s ease;
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
            /* Subtle white glow */
        }

        .button:hover {
            transform: scale(1.05);
        }

        /* Individual button colors */
        .menu {
            background-color: #64b5f6;
            /* Light blue for menu */
        }

        .menu:hover {
            background-color: #42a5f5;
            /* Slightly darker blue on hover */
        }

        .driver {
            background-color: #6a0fd3;
            /* Purple for driver */
        }

        .driver:hover {
            background-color: #812be4;
            /* Slightly darker purple on hover */
        }

        .admin {
            background-color: #4caf50;
            /* Green for admin */
        }

        .admin:hover {
            background-color: #43a047;
            /* Slightly darker green on hover */
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <div class="navbar">
        <h1>Tasty Bites Restaurant</h1>
    </div>

    <!-- Main Content -->
    <div class="content">
        <h1>Welcome to Our Service</h1>
        <div class="button-container">
            <a href="/menu" class="button menu">Restaurant Menu</a>
            <a href="/driver" class="button driver">Driver</a>
            <a href="/admin" class="button admin">Administrator</a>
        </div>
    </div>
</body>

</html>